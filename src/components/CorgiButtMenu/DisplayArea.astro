---
interface Props {
	title?: string;
	text?: string;
}

const { title = "Welcome!", text = "お尻をひっぱってメニューを選んでね🐾" } = Astro.props;
---

<div id="content-area">
	<h1 id="display-title">{title}</h1>
	<div class="text-pill">
		<p id="display-text" set:html={text}></p>
	</div>
</div>

<style>
	#content-area {
		position: relative;
		margin-top: 40px;
		text-align: center;
		min-height: 160px;
		display: flex;
		flex-direction: column;
		align-items: center;
		pointer-events: none;
		z-index: 10;
	}

	h1 {
		color: #f19d5dff; /* コーギーカラーのオレンジ */
		font-size: 2.8rem;
		font-weight: 900;
		margin-bottom: 15px;
		letter-spacing: 0.05em;
	}

	.text-pill {
		background-color: #f19d5dff;
		padding: 12px 20px;
		border-radius: 40px;
		width: 420px; /* 固定幅を設定 */
		max-width: 95%; /* スマホなど狭い画面への配慮 */
		display: flex;
		justify-content: center;
		align-items: center;
		box-shadow: 0 4px 15px rgb(233, 147, 81, 1);
	}

	p {
		color: white;
		font-size: 1.1rem;
		font-weight: 600;
		line-height: 1.5;
		margin: 0;
		/* 2行分のスペースを確保 */
		min-height: 3em; 
		display: flex;
		align-items: center;
		justify-content: center;
	}
</style>
